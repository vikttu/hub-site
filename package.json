{
  "name": "app",
  "private": "true",
  "dependencies": {
    "esbuild": "^0.14.39"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --external:@hotwired/* --bundle --sourcemap --outdir=app/assets/builds"
  }
}
